currently working on a maze generator algorithm:
. i'm going to implement both a 'backtracking' and 'prism' algorithms, that will work separately
. each cell of the maze will contain a number representing which walls are present.
. 4 LSB will represent each of the walls so a 1111 or 15 will mean that all walls are present

possible order of operations:
with:

class MazeCell():
    """
    pass
    """

    def __init__(self, index: int) -> None:
    	self.INDEX = index
        self.walls = 15
        self.static = False # if True it means 'walls' can't be modified or that it was already visited and no 'neighbors' could be 'visited'

    def del_north(self) -> None:
        """
        Deletes 'North' wall
        """

        self.walls = self.walls ^ 1

    def del_east(self) -> None:
        """
        Deletes 'East' wall
        """

        self.walls = self.walls ^ 2

    def del_south(self) -> None:
        """
        Deletes 'South' wall
        """

        self.walls = self.walls ^ 4

    def del_west(self) -> None:
        """
        Deletes 'West' wall
        """

        self.walls = self.walls ^ 8

1) generate an all blocked cells maze: maze = [MazeCell(elements) for elements in range(0, width * height)]
# both algorithms work similar:
2) use of a 'visited' cells list that starts empty:
 visited: list[MazeCell] = []
3) start by selecting a random cell from maze and adding it to 'visited':
def random_cell(
        cells: list[MazeCell],
) -> MazeCell:
    """
    Returns a non 'static' cell randomly selected from 'cells'
    """

    cell = cells[random.randint(0, len(cells) - 1)]

    if cell.static:
        return random_cell(cells)
    else:
        return cell
visited.append(random_cell(maze, width, height))
4) from what I understood, main difference between them lies in the selection of a cell from 'visited': prism selects randomly and backtracking works with the last one added
 # vague definitions just to make my point
 prism(random_cell(visited))
 backtracking(visited[-1])
5) proceed by selecting a 'neighbor' cell. It can be located up, right, down or left from selected 'visited' cell
movements_list = ['N', 'E', 'S', 'W']
. check if selected 'neighbor' is valid: it should be inside of the maze boundaries and shouldn't be already in 'visited' list. In my case it should also not be 'static'
. 


